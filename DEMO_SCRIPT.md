# Bazaar — Demo Video Cut Sheet (for Codex editing)

**Format:** screen-recording + voiceover ONLY — **no person on camera** (founder credit is a text lower-third).
**Length:** target **2:49** — HARD cap **< 3:00** (Lepton rejects over). Durations below sum to 169s.
**Spine:** PROOF-FIRST — open on a real on-chain slash tx, frame everything as independently verifiable, close on `npm run verify`.
**Hook (first words, no logo splash):** *"This is a real transaction on Arc. A seller just lost its USDC bond for bad AI work — and nobody at our company decided that. The chain did."*

**Visual grammar:** one monospace dark terminal theme · persistent top-bar caption strip (current claim) · persistent lower-third tx-chip mirroring the exact on-screen hash · **GREEN = paid/protected, RED = slashed/forfeit, AMBER = the twist** · green ✓ "truth-stamp" only AFTER an arcscan receipt visibly resolves · say jargon (commit-reveal / calibration-weighted median) exactly once, in plain English, with the term as a caption · VO ≤ 2.7 words/sec · kill music under the honesty card.

| # | Time | Sec | Criterion | On-screen | Voiceover | Caption |
|---|---|---|---|---|---|---|
| 1 | 0:00–0:13 | 13 | Traction | Arcscan tx `0x5469f51c…` full-screen (Status: Success already loaded). Overlay human-readable labels over the 18-dec wei: "seller bond −0.02000 USDC → buyer +0.03079 USDC", with a small reconciliation line so the math is instantly legible: "buyer refund = 0.02 slashed bond + 0.01079 call payment back". Red **BOND SLASHED** stamp. Corner: "Arc Testnet · chain 5042002". | *This is a real transaction on Arc. A seller just lost its USDC bond for bad AI work — and nobody at our company decided that. The chain did.* | REAL on-chain slash · 0x5469f51c · refund = 0.02 bond + 0.01079 call = 0.03079 |
| 2 | 0:13–0:25 | 12 | Intro | Title card over dim terminal: "BAZAAR — proof-of-quality nanopayments". Problem line types in. 3-icon strip: pay sub-cent USDC → grade → bad work = bond slashed. Text credit (no face): "Builder: prior Arc-hackathon winner · MSc Data Science, Sheffield". | *As AI agents start hiring other agents, two things are missing: paying a fraction of a cent per call, and knowing the paid work was actually good. Bazaar is that accountability layer — on Arc, native USDC.* | Bazaar · accountability layer for agent-to-agent AI |
| 3 | 0:25–0:30 | 5 | Circle | Architecture strip lights L→R: Arc·native USDC → Circle ERC-8004 IdentityRegistry → Circle DCW → CrucibleMarketV7 + ScalarResolverV10. | *It's composed, not reinvented: Circle's ERC-8004 identity, Circle wallets, and contracts already live on Arc. Zero new Solidity.* | Built on Arc + Circle + reused contracts · zero new Solidity |
| 4 | 0:30–0:44 | 14 | Agentic | `npm run trustless` — sped-up real log: bond deposit, "V1 0/100 via llm:deepseek-v4-pro", "V2 … deepseek-v4-flash". Badge: "⏩ sped-up · real 30+30-min windows". Side 3-node diagram. | *Here's the mechanism. A seller stakes a USDC bond and does real work. A buyer pays per call and can dispute. But the grade that moves money isn't ours — independent validators stake their own USDC and score the work.* | npm run trustless · staked validators · distinct model each |
| 5 | 0:44–0:58 | 14 | Innovation | Split: commit-reveal timeline bar ("commit 30m → reveal 30m · sped up, windows REAL"); the two real deliveries (BAD empty / GOOD 3-bullet) + vote chips 0,0. | *Validators commit a hashed score, then reveal. The payout is the median, so one biased validator can't swing it. This is pre-recorded and sped up — the protocol's real thirty-plus-thirty-minute windows, not a fake live timer.* | commit-reveal → median · footage sped up, windows REAL |
| 6 | 0:58–1:12 | 14 | Innovation/Traction | "TRUSTLESS RESULT" block: BAD CONSENSUS 0/100, bond slashed 0.02, to buyer 0.03079. Balance counter −0.02. Callback to arcscan 0x5469f51c resolving, green ✓. | *On the bad delivery, the validators reach consensus zero. The contract slashes the bond and refunds the buyer — the same transaction you saw first. No platform held the money. No operator typed the score.* | BAD → 0/100 → SLASHED 0.02 · 0x5469f51c |
| 7 | 1:12–1:18 | 6 | Innovation | AMBER twist card: "But what if the BUYER is the cheater?" → "A buyer disputes work that was actually GOOD — to dodge paying." | *Now the attack nobody demos: what if the buyer lies — disputes good work just to avoid paying?* | THE TWIST — what if the BUYER lies? |
| 8 | 1:18–1:33 | 15 | Agentic/Innovation | GOOD + ADVERSARIAL block: CONSENSUS 100/100, bond slashed 0, disputer bond to seller 0.001, "A dishonest buyer CANNOT slash an honest seller — PASS ✅". Arcscan 0x2d411523 green ✓. | *Validators read the actual work, agree it's a hundred out of a hundred. The seller's bond is protected — and the lying buyer forfeits its own dispute bond to the seller it tried to cheat. Asserted, on-chain.* | GOOD + LYING buyer → 100/100 · liar forfeits 0.001 · 0x2d411523 |
| 9 | 1:33–1:44 | 11 | Agentic | Validator-accountability: "⚖ validator 0x468Ac1 slashed 0.00882". **Inline honest caption:** "V3 deliberately FORCED off-consensus to demo the validator-slash path; the genuinely-distinct 3-live-model run is `npm run circle:trustless`." | *Validators are held to it too. To show the path deterministically, we force one validator off-consensus — and the chain slashes the validator itself, point-eight-eight cents. The fully live three-model run is a separate command.* | Validator-slash path (V3 FORCED, demo) → 0.00882 |
| 10 | 1:44–2:00 | 16 | Circle | `npm run circle:trustless` — real log: "open COMPLETE 0x7c9b913b", "dispute COMPLETE 0xf7ea1cbb", "V1 pro · V2 flash · V3 chat". Badge: "Circle DCW 0x9608… — Circle signs+broadcasts, we never hold the key". Arcscan 0x7c9b913b green ✓. Inline: "Circle role = OPEN + DISPUTE; resolution lands after windows." | *Circle isn't bolted on. A Circle Developer-Controlled wallet itself opens and disputes the bonded market through Circle's contract-execution API — Circle signs and broadcasts, we never hold the key. And this run uses three genuinely distinct live models.* | Circle DCW OPENS + DISPUTES the bonded rail · 0x7c9b913b / 0xf7ea1cbb · 3 live models |
| 11 | 2:00–2:14 | 14 | Traction/Circle | 3-up montage (~4.5s each), each ending on its arcscan tx ✓: (1) `circle:pay` → seller +0.002 · 0x4c6db2f9; (2) `beginner` → VERBATIM reason overlay, brain "anthropic:claude-opus-4-6" → 0x478a2402; (3) `byoa:ext` → independent agent 0x19D1… → 0xac74ffee. | *Beyond the bond: a Circle wallet makes a sub-cent payment. A Claude-powered newcomer with its own wallet reasons like a real first-timer and pays on-chain. And a separately-keyed external agent pays a seller. Real wallets, real fractions of a cent.* | Circle 0x4c6db2f9 · beginner 0x478a2402 · external 0xac74ffee |
| 12 | 2:14–2:26 | 12 | Traction (credibility) | Full-screen honesty card, plain text, **no music swell**: "HONEST SCOPE — Testnet only. Validators run distinct models on distinct keys but are TEAM-OPERATED today (mechanism permissionless; outside validator set is the open gap). External/beginner agents independently-keyed but self-funded. Reused contracts. Zero new Solidity." | *We'd rather under-claim. This is testnet. The validators run distinct models on distinct keys but are still team-operated today — the mechanism is permissionless; an outside set is the open gap.* | Honest scope: testnet · team-operated for now · zero new Solidity |
| 13 | 2:26–2:49 | 23 | Traction (climax) | **Browser opens `ccheh.github.io/bazaar`** — evidence cards render and badges flip **"✓ confirmed · block N"** one by one (live, read-only Arc RPC straight from the browser); summary chip lands **"✅ 8/8 confirmed live on Arc Testnet"**. ~1s flash of `npm run verify` in a terminal as the CLI equivalent. Final card: "Verify, don't trust · **ccheh.github.io/bazaar** · github.com/Ccheh/bazaar". | *You don't have to trust any of this. Open this page — your own browser confirms all eight payment and slash transactions live on Arc, with no install and no keys. Or run one command. Clone the repo and check it yourself. That's Bazaar: where the chain, not the platform, has the final word.* | ccheh.github.io/bazaar → 8/8 confirmed live · github.com/Ccheh/bazaar |

## On-chain proof plan (no fake live 60-min wait)
- Trustless commit/reveal windows shown as **pre-recorded, time-compressed** footage with a persistent "⏩ sped-up · real protocol windows" badge — honest about compression, never a faked live timer.
- Hard proof two ways: (a) arcscan cutaways where **Success visibly resolves** on screen (full hash in address bar, persistent tx-chip; never pre-stamp ✓ over an unloaded page); (b) **`npm run verify` run LIVE read-only** at the climax.

## Capture checklist (what the founder screen-records)
1. Pre-loaded arcscan FULL-hash pages (Status: Success) for: `0x5469f51c` (slash), `0x2d411523` (good+liar), `0x4c6db2f9` (Circle pay), `0x7c9b913b` (Circle open), `0xf7ea1cbb` (Circle dispute), `0x478a2402` (beginner), `0xac74ffee` (external) — record each visibly resolving.
2. `npm run trustless` end-to-end → time-compress; keep TRUSTLESS RESULT + ADVERSARIAL PASS readable.
3. `npm run circle:trustless` showing open/dispute COMPLETE + V1/V2/V3 = pro/flash/chat.
4. `npm run circle:pay`, `npm run beginner`, `npm run byoa:ext` for the montage (capture beginner reason VERBATIM).
5. `npm run verify` LIVE read-only to "=== N confirmed ===".
6. Graphics: architecture strip, title/honesty/twist cards, the "⏩ sped-up" badge, top-bar claim strip + lower-third tx-chip templates.

## Honesty guardrails (do NOT cross — judges verify)
- **V3 0.00882 slash is FORCED** (`via:'forced-outlier'`), not organic — scene 9 says so; point to `circle:trustless` for the genuinely-distinct 3-live-model run.
- **Circle role = OPEN + DISPUTE only** (resolution lands after windows) — don't claim Circle "drives the slash".
- **`npm run verify` now confirms 8/8** (incl. the Circle open/dispute/resolve txs — state backfilled), so "all eight transactions" in scene 13 is accurate. The live dashboard at **ccheh.github.io/bazaar** confirms the same 8 from a browser.
- Beginner brain is exactly `anthropic:claude-opus-4-6`; reason text VERBATIM; note 0.005 is the budget, 0.002 the payment.
- Always overlay human-readable USDC over 18-dec wei on arcscan rows. No on-camera person anywhere.

## Closing-shot capture (scene 13)
Screen-record **the live dashboard** at `https://ccheh.github.io/bazaar/` loading and the badges flipping to "✓ confirmed · block N" (it calls Arc RPC live from the browser — CORS verified). If a badge is slow, the page falls back to an Arcscan link, so always cut on a green/summary state. Also grab a ~1s `npm run verify` terminal pass as the CLI equivalent. The dashboard doubles as the "displayable" artifact — it is GitHub-hosted (Pages), not a live server, so it is not the submission's optional "live link".

## Editing notes
**Runtime is the one hard-fail risk:** the cut-sheet sums to 2:49 but the cap is a HARD < 3:00 (auto-reject).
**Target a 2:35–2:40 export, not 2:49** — pre-trim the wordiest VO (scenes 5 / 10 / 13, all near the 2.7 words/sec
ceiling), and **stopwatch the EXPORTED file** (not the cut-sheet sum) before submitting. Never cut scene 12 (honesty)
or 13 (verify). Keep ≤1 technical term per on-screen caption (move the rest to VO). For variety in the 0:44–1:33
mechanism stretch, swap one terminal beat for the README mermaid CORE-box animation so scenes 5–9 aren't four identical
log shots. Capture a **fallback "already-green" dashboard** frame for scene 13 in case the live RPC is slow at record time.
