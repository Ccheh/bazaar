node_modules/
.env
.env.*
!.env.example
*.local
dist/
keys/
*.log
# runtime claim queue(s) + ledgers
pending-claims.jsonl
*.queue.jsonl
*.ledger.jsonl
.DS_Store


# Circle secrets (NEVER commit) — wallet list (addresses) IS committed
circle-entity-secret.txt
circle-recovery.dat
*.secret

.claude/
